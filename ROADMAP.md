# Roadmap

1. Capture rendered evidence for a named facing candidate and a named pattern candidate, then keep deterministic runtime proof separate from human-observed appearance.
2. Add only the next surface vocabulary rule demonstrated by a real request; do not invent arbitrary breadth in advance.
3. Add negative and budget fixtures discovered by real execution.
4. Keep every candidate inspectable and every unsupported request explicit.
5. Re-test before widening the declared MorphTile compatibility range.
6. Do not expose MorphTile's generic pattern fallback/noise branch as creation vocabulary until a real request earns a named contract.

Do not add speculative breadth merely to make the repository look complete.
